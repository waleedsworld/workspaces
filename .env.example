# Copy to .env and tweak as needed. All values are optional.

# Interface to bind to (0.0.0.0 exposes it on your network).
HOST=0.0.0.0

# Port the API listens on.
PORT=5000

# Set to "true" to enable Flask's auto-reloader and debugger.
# Keep this "false" in production.
FLASK_DEBUG=false
